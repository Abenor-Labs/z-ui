{
  "name": "@z-ui/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Registry build output. Reads registry/ and writes public/r/ for the CLI. The website itself lives in site/.",
  "scripts": {
    "registry": "node scripts/build-registry.mjs",
    "registry:check": "node scripts/build-registry.mjs --check"
  },
  "devDependencies": {
    "shiki": "^4.4.1"
  }
}
