{
  "name": "@z-ui/site-orbs",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "description": "The single-route Z-UI showcase. One page: the eight components, a terminal that writes the install command for whichever you point at, and click-to-copy. Separate from site/ (the full multi-route site).",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -b"
  },
  "dependencies": {
    "motion": "^12.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.7.2",
    "vite": "^6.0.7"
  }
}
